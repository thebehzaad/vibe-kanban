# Docker Compose Dev Environment
# Copy this to .env and adjust paths for your machine

# Host path to your git repos (mounted into backend container at /repos)
GIT_REPOS_PATH=~/git_repos

# Ports
FRONTEND_PORT=3000
BACKEND_PORT=3001

# Rust log level
RUST_LOG=debug
